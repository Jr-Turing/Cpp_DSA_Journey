/* 
== -> equal to equal. 
> -> grater than.
< -> smaller than.
>= -> grater than equal to.
<= -> smaller than equal to.
!= -> not equal to.
*/

#include<iostream>
using namespace std;

int main()
{
    // comparison operator
    // 1 or 0

    int a,b;
    cout<<"Enter the value of a: ";
    cin>>a;
    cout<<"Enter the value of b: ";
    cin>>b;

    // ==

    // if(a==b)
    // cout<<"yes";
    // else
    // cout<<"no";

    // > , < , >= , <=

    // if(a>b)
    // if(a<b)
    // if(a>=b)
    // if(a<=b)
    // cout<<"yes";
    // else
    // cout<<"no";

    // !=
    if(a!=b)
    cout<<"yes";
    else
    cout<<"no";



}